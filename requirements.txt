# Core
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
librosa>=0.10.0
soundfile>=0.12.0
pyyaml>=6.0

# Spoof Generation (Phase 1)
TTS>=0.22.0              # Coqui TTS — includes XTTS v2
# openvoice — install from git: pip install git+https://github.com/myshell-ai/OpenVoice.git
# styletts2 — install from git: pip install git+https://github.com/yl4579/StyleTTS2.git

# Evaluation (Phase 2)
scikit-learn>=1.3.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Detectors (Phase 2)
# transformers>=4.30.0   # For Wav2Vec2/Whisper-based detectors
# fairseq                # For wav2vec2 features (optional)

# Utilities
tqdm>=4.65.0
jsonlines>=3.1.0
